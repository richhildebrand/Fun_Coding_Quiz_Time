var poor_naming_conventions = function(outsideText, insideText) {

    var text = outsideText;
    let logIt = function() {
        return text;
        var text = insideText;
    };

    return logIt();
}